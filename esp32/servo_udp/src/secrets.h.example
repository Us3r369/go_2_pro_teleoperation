#pragma once
// Copy this file to "secrets.h" (same directory) and fill in your network.
// secrets.h is gitignored — never commit real WiFi credentials.
//
// Put the laptop bridge and this board on the SAME network (the project uses
// the driver's LocalSTA mode for the multi-device setup).

#define WIFI_SSID "your-network-name"
#define WIFI_PASS "your-network-password"
